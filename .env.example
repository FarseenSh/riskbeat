# Pharos (stablecoin monitoring feed) — free self-serve key: https://pharos.watch/api/
# Without a key the Pharos fetcher degrades gracefully (cells render not-yet-covered).
PHAROS_API_KEY=

# Optional: raises GitHub raw rate limits for the DeFiScan / DeFiPunk'd fetchers
# (unauthenticated works, at 60 req/hr).
GITHUB_TOKEN=
